package work3;

/**
 * The GraphicElement interface defines a method for drawing graphic elements.
 */
public interface GraphicElement {
    /**
     * Draws the graphic element at the specified coordinates.
     *
     * @param x the x coordinate
     * @param y the y coordinate
     */
    void draw(int x, int y);
}
